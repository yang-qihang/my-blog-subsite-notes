[← Back to Vim Directory](../index.md){ .md-button }

# My Neovim Setup

1. Create the config file
     ```bash
     mkdir -p ~/.config/nvim
     nvim ~/.config/nvim/init.lua
     ```

2. Add the following lines in `init.lua`

     ```bash
     vim.opt.guicursor = ""
     ```
